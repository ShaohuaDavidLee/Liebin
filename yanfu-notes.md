# 严复译注

> 任务包 v2 · `sourceLanguage: auto` 检测为中文 → `targetMarket: china` → `targetLanguage: zh-CN`，路径为**中→中**（面向中文市场重述）。
> 原页：<https://liebin.caojuege.com/>（仓库内 `site/index.html`）。交付：`landing.html`、`yanfu-notes.md`。

## 信

- **原始价值主张：** 列宾是一个 Claude Code / Claude Skill。动手写页面之前，它先用用户的真实文案渲染三个变体 × 三屏的样张，逼用户做出取舍；把用户"选了什么、否掉了什么、为什么"逐字写进一份 DESIGN.md，交给会写代码的 agent。**它不接管开发**，DESIGN.md 交付即完工。

- **忠实保留：** 品牌名与域名、GitHub 仓库地址、两条真实安装命令（全局 / 项目内）与复制交互、任务包表单与 zip 下载、支持的 agent 名单（Claude Code、Codex、Cursor、Grok、Openclaw、Kimi Work、WorkBuddy、Hermes、DeepSeek harness）、四个坑的具体内容（含 `-2.46px` → `-0.031em` 那个真实例子）、列宾两个夏天习作的典故与两张公版画作及其出处、草诀歌 AI Labs / 韶华David / 共创班致谢、DESIGN.md 规范来自 google-labs-code/design.md（Apache-2.0）。

- **表达依据：**

  | 新页面表达 | 原始依据 | 处理 |
  |---|---|---|
  | H1「先做三版，你挑一版，再写代码。」 | README「在动手写页面之前，先把设计方向做成三张能被否决的样张」+ SKILL.md 四步 | 重组 |
  | 首屏引语「做出来的和我想要的不是一个东西，但我说不清哪里不对。」 | README / SKILL.md 原文 | 逐字保留 |
  | 「给 Claude Code 这类 AI 编程工具装的一个 skill」 | README「一个 Claude Code / Claude Skill」+ 安装路径 `~/.claude/skills/liebin` | 保留 |
  | 「你说不出口的 / 你一眼能指出的」对照 | SKILL.md「用户说得出'这个圆角太软了''这个字太挤了'，说不出'我要什么'」 | 重组 |
  | 四步卡片文案 | README 四步表 + SKILL.md 各步细则 | 压缩 |
  | 「一个例子」整节 | 上传的 `liebinlandingdirections.html`（三变体 × 三屏 + 高/中/低信息密度轴）与 `liebinlandingDESIGN.md`（`selected_variant: 碑文`、Don'ts 首条原话、日期 2026-08-17） | 新增（全部有据） |
  | FAQ 五问 | README「它不做什么」「三个刻意的设计」「怎么用」+ 原页 install-note | 重组 |

- **因证据不足未使用：** 任何用户数、下载量、节省时间、准确率、满意度、客户 logo、评价；也没有把 agent 名单写成"支持 9 种工具"这类数字承诺（原文只是一份名单）。页面**没有**声称"你正在看的这张页面是照那份 DESIGN.md 生成的"——见「待确认」第 1 条。

## 达

- **最终一句话 Pitch：** 先做三版，你挑一版，再写代码。

- **故事主线：** 首屏（是什么 / 给谁 / 怎么用）→ 你遇到的问题（说不出口的 vs 一眼能指出的）→ 四步机制 → 一个真实例子（三版样张 → 一句原话 → DESIGN.md）→ 为什么叫列宾 → 它替你盯住的四件事 → 常见问题 → 怎么开始（安装 + 任务包 + 交接）。

- **信息顺序调整：**
  - README 里最有杀伤力、但原落地页**根本没写**的那句用户困境，提到首屏第二行。这是本次最大的一处"达"。
  - 「为什么叫列宾」（习作与定稿）从第 2 屏移到证据之后——它是信任层，不是理解层。
  - 安装、任务包表单、交接说明从中段移到收尾，作为唯一的最终 CTA；首屏按钮直接锚到那里。
  - 新增「一个例子」与「常见问题」两节：前者把抽象的"三版样张"变成看得见的东西，后者接住目标用户（自己 vibe coding 做产品的人）最可能的五个疑问。

- **删除、合并或弱化：**
  - **删除原 H1「让大师帮你『偷』设计。」及「优秀的艺术家模仿，伟大的艺术家偷窃」引语与拉丁旁注。** 两个理由：一，陌生人五秒内读不出这是什么产品、给谁、解决什么；二，"偷设计"指向复刻与提取，而产品在 README 里明确写着「不负责提取设计系统……提得再准也解决不了开头那个问题」，修辞与定位相反。这是全篇最重的删减，特此标出。
  - 内部词改成用户词：三变体 × 三屏 → **三版 × 三屏**；核心操作屏 → **主要操作页**；空状态 → **没内容时**；任务包 → **一包材料**（zip 与 BRIEF.md 仍在文中说清）。
  - 首屏原本两个 CTA 都在讲流程（「装上，攒一个任务包」/「四步是什么」），合并为一个动作（装上列宾）加一个证据入口（先看一个真实的例子）。
  - 四个坑的标题由名词改为动作（数值崇拜 → 照抄参考里的数字），整节从"坑"改写为"它替你盯住的四件事"，让它落在用户结果一侧。

## 电梯演讲

- **30 秒版本：**
  > 列宾是给 Claude Code 这类 AI 编程工具装的一个 skill。你想做个页面，直接说「照着这个网站做一个」，它不会马上开写，而是先用你自己的真实文案渲染三版设计、每版三屏，并列摆在一张网页上。你挑一版，再说说另外两版哪里不要——比如「这个圆角太软了」。你说的那句原话会被逐字写进一份 DESIGN.md，你把这份文件交给 AI 去写代码。它只管动笔前那一步，不替你开发产品。

- **使用场景：** 对同行一句话说清（"先做三版，你挑一版，再写代码"）；向没用过 skill 的人解释它到底插在流程的哪一节；在群里回答"这和那些设计提取器有什么区别"。

- **不应越界承诺：** 不说它能提升转化、节省多少时间、保证好看；不说它会写网站；不说它已经被多少人用；不说它替你想清楚需求——它只把你的判断固定下来。

## 雅

- **保留的设计元素：** 直接沿用 `site/css/liebin.css`，**一行未改**。纸白 `#f7f7f4` / 墨 `#26251e` / 朱红 `#f54e00` / 胭脂 `#b3003f` / 金褐 `#c08532`；Noto Serif SC + Inter + EB Garamond 三体协作；纸面颗粒；胶囊按钮；`rn-kicker` + 金线的章节头；`rn-photo` 肖像框与图注胶囊；深色终端块。反色（`on-ink`）区仍然只用两处——为什么叫列宾、页脚——遵守 CSS 里"全篇 1–2 处即可"的注释。`site/js/app.js` 的全部 DOM 钩子与按钮文案原样保留，安装命令切换、复制、拖图、打包下载 zip 均已实测可用。

- **产品截图选择：** 三张，来自上传的 `liebinlandingdirections.html` 中三版样张的首屏（碑文 / 留白 / 静页），按 1280×700 渲染为 `site/img/case/sample-*.jpg`。**它们不是"证据截图"，而是这个产品的产出物本身**——不看见"三版并列"长什么样，用户无法理解这个 skill 的形态，因此按输出规范克制展示，配的是用户导向的说明文案，页面上没有一处"截图可见""界面显示"这类译注式说法。DESIGN.md 片段直接引原文，未改写。

- **忠于原 Landing Page 的设计风格：** 新增的三处版式（对照卡、案例栅格、问答行）全部复用既有 token 与既有组件语言——`.pit` 卡、hairline 分隔行、`.term` 的深色块尺度——没有引入新颜色、新字族、新圆角尺度或第三层阴影，视觉密度与原页一致。

- **必要且克制的视觉修正：**
  - 首屏主句与引语加了显式 `<br>`：`h1` 的 `max-width:14ch` 在 Noto Serif SC 下约合 7 个汉字，中文长句会把「设计」这类词拆到两行。这是中文排版修正，不是版式改动。
  - 三张样张图带 `width`/`height`，避免加载时跳动。
  - 桌面 1440px 与移动 390px 实测无横向溢出，`h1` 全篇唯一，站内锚点全部有效。

## 待确认

- **需要开发者确认：**
  1. **案例区的定性（最高优先级）。** 上传的 DESIGN.md 里 `selected_variant: 碑文`，锁定的视觉是「留白」水彩语言；而线上这张落地页用的是文艺复兴纸墨语言。两者是不是同一条线，我无法从材料判断。所以页面只说「这三版，是列宾给自己做的」，没有说「你正在看的这一页就是照这份 DESIGN.md 生成的」。如果确实是同一条线，这句话可以说得更强，说服力会明显上一个台阶。
  2. **「另外两个hero传达的信息太少」按原样保留**——没有加空格、没有补句号。按产品自己的主张（原话不做润色），我建议维持；如果你希望排版整齐，请明确授权，因为那一改就是润色。
  3. 首屏主按钮「装上列宾」锚到 `#how`，与原页行为一致。如果你更希望首屏直接给出可复制的安装命令，可以换成那个动作。

- **暂未进入页面：**
  - 「照搬 / 取气 / 反手」只作为首屏示意图里的标签出现。README 里"手上有什么 → 缺什么 → 沿哪条轴分"那张完整的四行表没有展开成一节，只在 FAQ 里带过一句。要不要展开，取决于首页该承担多少说明责任——这正是那份 DESIGN.md 已经回答过的问题（选「碑文」，嫌另外两版说得太少），所以我把首屏做得偏密而不是偏空。
  - SKILL.md 提到的演示文稿（PPT）扩展：没有对应素材与证据，未提。
  - 一切数字、用户量、案例数量。

- **文件位置：** `landing.html`（引用 `./site/css/liebin.css`、`./site/js/app.js`、`./site/img/`），`yanfu-notes.md`，新增图片在 `site/img/case/`。要上线的话，把 `landing.html` 移成 `site/index.html` 并把四处 `./site/` 前缀改成 `./` 即可。
